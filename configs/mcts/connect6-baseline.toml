# Calibrated on Connect6 13x13; provisional baseline for GUI defaults.
# This agent profile does not configure the board size.
name = "connect6-baseline"
time_budget = 1.0
rollout_depth = 1024
exploration = 0.5
selection_policy = "uct_rave"
rave_equivalence = 20000
cutoff_evaluator = { kind = "neutral" }
rollout_policy = { kind = "uniform_random" }
tree_reuse = false
transpositions = false
